import { BASE_URL } from "../../../shared";

export const CountChannel = {
  NUTRISTIONIST_COUNT: `${BASE_URL}/utility/n/count/:Id`,
  CLIENT_COUNT: `${BASE_URL}/utility/c/count/:Id`,
  ADMIN_COUNT: `${BASE_URL}/utility/a/count/:Id`,
};
