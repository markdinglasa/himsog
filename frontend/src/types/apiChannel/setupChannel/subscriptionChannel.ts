import { BASE_URL } from "../../../shared";

export const SubscriptionChannel = {
  SUBSCRIPTION: `${BASE_URL}/setup/subscription`,
  SUBSCRIPTION_ID: `${BASE_URL}/setup/subscription/:Id`,
  SUBSCRIPTION_NAME: `${BASE_URL}/setup/subscription/name/:Id`,
};
