import { BASE_URL } from "../../../shared";

export const IngredientChannel = {
  INGREDIENT: `${BASE_URL}/setup/ingredient`,
  INGREDIENT_ID: `${BASE_URL}/setup/ingredient/:Id`,
  INGREDIENT_USER: `${BASE_URL}/setup/ingredient/user/:Id`,
};
