export enum Channels {
  AUTH = "/auth",
  SETUP = "/setup",
  TRANSACTION = "/transaction",
  UTILITY = "/utility",
  SETTINGS = "/settings",
  REPORT = "/report",
  LOGIN = "/login",
  LOGOUT = "/logout",
  REFRESH = "/refresh",
}
