import styled from "styled-components";
import { Badge as bdge, IconButton as ib, Tooltip as tt } from "@mui/material";

export const Container = styled.div``;
export const Badge = styled(bdge)(() => ({}));
export const IconButton = styled(ib)(() => ({}));
export const Tooltip = styled(tt)(() => ({}));
